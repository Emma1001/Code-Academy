#include <stdio.h>

int main(){
    
    int nA;
    scanf("%d", &nA);
    int nB = (nA == 1 ? 2 : 0);
    int maxNumber = (nA > nB ? nA : nB);
    
    printf("A value is %d\n", nA);
    printf("B value is %d\n", nB);
    printf("The bigger number is: %d\n", maxNumber);
    return 0;
}
